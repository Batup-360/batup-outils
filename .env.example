# Google Analytics 4 — même propriété que batup.fr (ID G-8NVKZYY1SE baké dans
# src/lib/analytics.ts). Cette variable n'est nécessaire que pour SURCHARGER
# l'ID au build (ex. propriété de test). Laisser vide en production.
# GA ne se déclenche que sur les hôtes *.batup.fr (localhost/previews silencieux).
VITE_GA_MEASUREMENT_ID=
